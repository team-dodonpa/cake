<div class="container "> 
 <div class="row">
  <div class="col-6">
<%= attachment_image_tag @item, :image, :fill, 500, 500 %>
  </div>
  <div class="col-6">
  <p>商品名：<%= @item.name %></p>
  <p>商品説明: <%= @item.introduction %></p>
   <p>ジャンル：<%=@item.genre.name %></p>
  <p>税抜き価格：<%= @item.price %></p>
  <p><% if @item.is_active == true %><p class="text-success">販売中</p>
 <% else %>　販売停止
 <% end %>
 <%= link_to "削除", admin_item_path(@item), method: :delete ,class:"btn btn-danger"%>
  </div>
 
 </div>
 <%= link_to "編集", edit_admin_item_path(@item), class:"btn btn-success"%> /
 <%= link_to "一覧へ", admin_items_path(@item) %>
</div> 